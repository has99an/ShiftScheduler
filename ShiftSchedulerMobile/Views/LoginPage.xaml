<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ShiftSchedulerMobile.Views.LoginPage">

    <StackLayout Padding="20">
        <Label Text="Login" FontSize="30" HorizontalOptions="Center"/>

        <Entry Placeholder="Email"
               Text="{Binding Email}" />

        <Entry Placeholder="Password"
               Text="{Binding Password}" 
               IsPassword="True" />

        <Button Text="Login"
                Command="{Binding LoginCommand}" />

        <Label Text="{Binding IsLoginSuccessful, Converter={StaticResource BoolToVisibilityConverter}}"
               IsVisible="{Binding IsLoginSuccessful}"/>
    </StackLayout>
</ContentPage>
